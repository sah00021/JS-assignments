var username;

var myName = "Abdul Hai";

// Step a) Declare a JS variable titled message
var message;

// Step b) Assign "Hello World" to variable message
message = "Hello World";

// Step c) Display the message in alert box
alert(message);

// Student's bio data
var studentName = "Abdul hai";
var studentAge = 21;
var studentGrade = "A";
var studentSchool = "XYZ High School";
var studentAddress = "Quarter C-4/5 malir colony";

// Displaying the data in alert boxes
alert("Student's Name: " + studentName);
alert("Student's Age: " + studentAge);
alert("Student's Grade: " + studentGrade);
alert("Student's School: " + studentSchool);
alert("Student's Address: " + studentAddress);

// Define a variable to hold the message
var alertMessage = "PIZZA\nPIZZ\nPIZ\nPI\nP";

// Display the alert using the variable
alert(alertMessage);

// Declare a variable called email and assign the email address
var email = "abdulhai214211@gmail.com";

// Create the message using string concatenation
var message = "My email address is : " + email;

// Display the message in an alert box
alert(message);

// Declare a variable called book and assign it the value
var book = "A smarter way to learn JavaScript";

// Display the message in an alert box
alert("I am currently reading the book : " + book);

// Write a script to display this in browser through JS
var content = "Yah! i can write HTML content through JS"
document.write(content)

// Store the given string in a variable
var specialString = "▬▬▬▬▬▬▬▬▬ஜ۩۞۩ஜ▬▬▬▬▬▬▬▬▬";

// Display the string in an alert box
alert(specialString);

// Display the string in the browser
document.write("<p>" + specialString + "</p>");